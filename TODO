Todo List:

---------------------------------------------------------------------

* Not happy with multiple nsopenssl's running at the same time:

It appears that functions such as NsOpenSSLGetModuleName simply
grab it from the first module in the driver linked list. Not a good
thing.

From what I see in modload.c, Ns_ModuleLoad calls the module's init
proc and passes it the server, module names. The module name in this
case should be coming from the section in nsd.tcl where the modules
are loaded, and it appears that this is where I can grab and store
the module's name in a static var for use later.



---------------------------------------------------------------------

* Should probably fix form.tcl to do the right thing.

form.tcl doesn't respect the fact that a boundary
value declaration may be wrapped in double quotes. It will work
with this:

   multipart/form-data, boundary=--123456789

but not this:

   multipart/form-data, boundary="--123456789"

       
---------------------------------------------------------------------

* Change EOF / peer aborts connection error message to be a notice or
  debug statement.

---------------------------------------------------------------------

* Add IP to EOF's and other errors logged.

---------------------------------------------------------------------

* Update the documentation to reflect latest changes for 2.1.

---------------------------------------------------------------------

* Take out nsdserver's message about client cert being valid.

